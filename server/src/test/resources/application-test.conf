ktor {
  application {
    modules = [com.example.voteapp.server.ApplicationKt.module]
  }
  deployment {
    port = 8080
  }
}

database {
  url = "jdbc:h2:mem:test;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH"
  driver = "org.h2.Driver"
  user = "sa"
  password = ""
}

firebase {
  enabled = false
}

